.header
  = image_tag @thing.image[:thumb].url if @thing.image.exists?

  %h1
    #{@thing.name}
    = link_to "Edit", edit_thing_path(@thing)

  By
  - @thing.authors.each do |author|
    = author.email

-# c/u should be passed via a Twin that is set up in the top Op/Comp.
= concept("rating/cell/form", @form, current_user: current_user).call

%ul
  - @thing.ratings.reverse.each do |rating|
    %li
      = concept("rating/cell/row", rating).call
